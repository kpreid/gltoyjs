This is a wrapper effect which executes fragment shaders written for the GLSL Sandbox <http://glsl.heroku.com/>. It will not run without providing a fragment shader.

It should be refactored so that it is a function from GLSL text to effect.